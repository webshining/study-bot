version: "3.9"

services:
  app:
    build: .
    restart: always
    entrypoint: ./bin/entrypoint.sh
    depends_on:
      - redis
  redis:
    image: redis
    volumes: 
      - ./redis.conf:/usr/local/etc/redis/redis.conf
    ports:
      - "6379:6379"